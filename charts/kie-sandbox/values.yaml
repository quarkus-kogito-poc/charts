---
enabled: true
kie_sandbox_namespace: kie-sandbox-namespace

kie_sandbox:
  version: 0.16.0
  # org: kie-tools
  org: kogito_tooling_bot
  replicas: 1
  route: true

cors_proxy:
  replicas: 1
  route: true

kie_sandbox_extended_services:
  replicas: 1
  route: true
